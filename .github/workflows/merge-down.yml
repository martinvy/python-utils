name: Merge down
on:
    push:
        branches:
            - master
            - staging
jobs:
    merge-down:
        runs-on: ubuntu-latest
        steps:
            -   uses: actions/checkout@master

            -   name: Merge Master -> Staging
                uses: devmasx/merge-branch@master
                env:
                    INPUT_MESSAGE: Merge branch master to staging
                with:
                    type: now
                    from_branch: master
                    target_branch: staging
                    github_token: ${{ github.token }}

            -   name: Merge Staging -> Dev
                uses: devmasx/merge-branch@master
                env:
                    INPUT_MESSAGE: Merge branch staging to dev
                with:
                    type: now
                    from_branch: staging
                    target_branch: dev
                    github_token: ${{ github.token }}
